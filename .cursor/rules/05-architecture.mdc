---
description:
globs:
alwaysApply: false
---
# 系统架构

## 总体架构

密码管理系统采用分层架构：
1. 表示层 (UI) - 用户界面
2. 业务层 (Core) - 业务逻辑
3. 数据层 (Data Storage) - 数据持久化

## 数据流

### 用户认证流程
1. 用户通过UI输入账号密码
2. `core/user.py`进行认证
3. 认证成功后显示主界面

### 密码管理流程
1. 用户通过UI执行密码相关操作
2. `core/password.py`处理业务逻辑
3. `core/encrypt.py`负责加解密
4. `core/data_storage.py`存储数据

### 数据存储流程
1. 应用层调用`data_storage.py`的统一接口
2. 根据配置选择本地存储或MySQL存储
3. 数据加密后存储

## 模块依赖关系

- UI模块依赖核心模块
- 核心模块使用工具模块
- 特性模块扩展核心模块功能
- 所有模块共享配置信息
