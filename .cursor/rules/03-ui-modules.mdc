---
description:
globs:
alwaysApply: false
---
# UI模块详解

密码管理系统基于PyQt5构建用户界面，分为几个主要模块：

## 登录界面

`ui/login/` 目录包含登录相关UI组件：
- 用户登录窗口
- 用户注册窗口
- 密码重置功能

## 主密码管理界面

`ui/password_manager/` 目录包含主要的密码管理界面：
- 密码列表展示
- 密码搜索和过滤
- 密码添加、编辑和删除
- 导入导出功能

## 对话框组件

`ui/dialogs/` 目录包含各种对话框：
- MySQL配置对话框
- Excel导入导出对话框
- 密码生成器对话框

## UI组件

`ui/components/` 目录包含可复用的UI组件：
- 自定义表格
- 自定义按钮
- 信息提示组件

## UI设计原则

本项目遵循以下UI设计原则：
1. 简洁明了，减少用户操作步骤
2. 一致的视觉风格
3. 响应式布局
4. 合理的错误处理和用户提示
